@inherits LayoutComponentBase
@inject HttpClient Http

<p class="backgroundDark">
    Lorem ipsum dolor sit amet, consectetur adipisicing elit. Accusantium eaque eius, ipsam itaque laudantium molestiae necessitatibus praesentium qui quia veniam.
</p>
<div class="backgroundPaper" style="font-size: @FontSizeString">
    <nav class="px-5 bg-light d-block">
        <span>Font Size: @FontSize</span>
        <button @onclick="IncreaseSize" class="btn btn-primary">+</button>
        <button @onclick="DecreaseSize" class="btn btn-secondary">-</button>
        <span class="ms-3">Background:</span>
        <select class="form-select w-auto d-inline" @bind="@Background">
            <option value="">Light</option>
            <option value="backgroundPaper">Paper</option>
            <option value="backgroundDark">Dark</option>
        </select>
    </nav>
    @Body
</div>

@code
{
    public string FontSizeString { get; set; } = "15pt";
    public string Background { get; set; } = "";
    public int FontSize { get; set; } = 15;
    public void IncreaseSize()
    {
        FontSize++;
        FontSizeString = $"{FontSize}pt";
    }
    public void DecreaseSize()
    {
        FontSize--;
        FontSizeString = $"{FontSize}pt";
    }


    protected override async Task OnInitializedAsync()
    {
        await base.OnInitializedAsync();
    }
}
